import Hero from "@/components/Hero";
import Navbar from "@/components/Navbar";

const page = () => {
  return (
    <>
      <section id="heroSection">
        <Navbar />
        <Hero />
      </section>
    </>
  );
};

export default page;
